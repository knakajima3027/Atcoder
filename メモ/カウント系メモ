リストに格納されている特定の要素の数を高速で出す
from collections import Counter
d = Counter() 
d.update(arr)

例）
from collections import Counter

arr = [1,1,4,6,1,1,35,1,5,1,3]

d = Counter() #インスタンスを生成
d.update(arr)

print(d[1]) #d[数えたい値]

--実行結果--
6
