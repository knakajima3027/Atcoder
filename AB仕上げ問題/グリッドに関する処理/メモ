グリッドに関する処理  
競プロで超頻出の形式(らしい)。就活のwebテストでも見た気がする形式。
入力としてまず、行と列の数が与えられる。行数をH、列数をWとする。
H = 3, W = 4とすると

..#.
#...
.##.

といった感じの入力がくる.

#を爆弾として,各座標に隣接する爆弾の数を求めろ(ABC075_B)というような問題が出る.

--入力の設定(Python)--

W, H = map(int,input().split()) 入力として, W, Hをそれぞれ格納

arr = [input() for j in range(W)] 文字数Hの文字列が、W個含まれるリストにして扱う

競プロはエラー処理が求められてないみたい
例) W = 2, H = 2 という設定で
..#.
..#.
..#.
といった4行４列の入力がくる
..#
.
といったそもそも行列になっていない形式などといった入力はこない
